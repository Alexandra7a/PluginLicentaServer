name: CI Pipeline
on: [push, pull_request]

jobs:
  build-test-lint:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    # ENVIRONMENT SETUP
    - name: Setup Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    # DEPENDENCY CACHING
    - name: Cache pip dependencies
      uses: actions/cache@v3
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements.txt') }}

    # LINTING STAGE
    - name: Install Flake8
      run: pip install flake8

    - name: Run Linter
      run: flake8 .
      # QUALITY GATE: Fails on style violations

    # BUILD STAGE
    - name: Build Docker image
      run: docker build -t vulnerability_detection_app .
      # QUALITY GATE: Fails on Docker build errors

    # TEST STAGE
    - name: Install Testing Dependencies
      run: pip install pytest

    - name: Run tests
      run: pytest
      # QUALITY GATE: Fails on test failures

    # DEPLOYMENT STAGE (if applicable)
    - name: Deploy to Docker Registry
      run: |
        echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USERNAME }}" --password-stdin
        docker push vulnerability_detection_app
      # Ensure to use secrets for Docker credentials